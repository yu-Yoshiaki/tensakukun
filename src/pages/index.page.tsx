import type { CustomNextPage } from "next";
import { Layout } from "src/component";

const Root: CustomNextPage = () => {
  return (
    <Layout header="Top">
      <div></div>
    </Layout>
  );
};

export default Root;
